
export default {
    getAppURL({ commit }){
        console.log(commit)
        // appDownloadPageUrl({ domain: window.location.host }).then(res=>{
        //     commit('CHANGE_TEXT', res.body)
        // })
    }
}